class Formulas:

    def __init__(self):
        self.k = '3'
        self.conjunto = None

    def fit_k_means(self, pontos, parada, max_iter):
        self.k = '3'

    def calcula_hipotenusa(self, a: float, b: float):
        hipotenusa = ((a * a) + (b * b))
        return hipotenusa
